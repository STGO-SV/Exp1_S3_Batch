spring.application.name=banco-legacy-auth
server.port=${AUTH_PORT:8084}
server.address=${AUTH_ADDRESS:127.0.0.1}
security.jwt.private-key=${JWT_PRIVATE_KEY}
security.jwt.public-key=${JWT_PUBLIC_KEY}
security.jwt.issuer=${JWT_ISSUER:banco-legacy-auth}
security.jwt.audience=${JWT_AUDIENCE:banco-bff}
security.jwt.ttl-seconds=${JWT_TTL_SECONDS:300}
demo.web-password=${DEMO_WEB_PASSWORD}
demo.mobile-password=${DEMO_MOBILE_PASSWORD}
demo.atm-password=${DEMO_ATM_PASSWORD}
# TLS directo; PKCS12 local generado con el procedimiento del profesor.
server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=${TLS_KEYSTORE_PASSWORD:changeit}
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=bff-local
